  <% if user_signed_in? %>
  <div class="indexpost">
    <%= link_to "New Post", new_post_path, class: "btn btn-primary" %>    
  </div>
  <br />
  <br />
  <%= form_tag(posts_path, :method => "get", id: "search-form") do %>
    <%= text_field_tag :search, params[:search], placeholder: "Search User" %>
    <%= submit_tag "Search" %>
  <% end %>
  <div class="row clearfix">
    <%= render @posts %>
  </div>
  <!-- <table class = "table table-bordered table-striped">
    <thead>
      <tr>
        <th>User</th>
        <th>Title</th>
        <th>Download Link</th>
        <th>Action</th>
      </tr>
    </thead>

    <tbody>
      <% @posts.each do |post| %>

        <tr>
          <td><%= link_to post.user.name, post.user %></td>
          <td><%= link_to post.title, post %></td>
          <td>
            <%= link_to post.attachment_url do %>
              <%= image_tag post.attachment_url(:thumb).to_s %>
            <% end %>
          </td>
          <% if current_user.admin? || current_user == post.user %>
            <td><%= button_to "Delete",  post, method: :delete, class: "btn btn-danger", confirm: "Are you sure that you wish to delete #{post.title}?" %></td>
          <% end %>
        </tr>

      <% end %>
    </tbody>
     
  </table> -->
<% end %>